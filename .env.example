# Development (MailHog)
MAIL_SERVER=localhost
MAIL_PORT=1025
MAIL_USERNAME=gaaligowthami22@gmail.com
MAIL_PASSWORD=yvzk ofaa kmbx mnyw
MAIL_FROM=dev@example.local

# Production (Gmail example - use App Password)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=you@gmail.com
# MAIL_PASSWORD=<APP_PASSWORD>
# MAIL_FROM=you@gmail.com

# If using port 465 (SSL/TLS): set MAIL_PORT=465 and the mail config will enable SSL
# For Gmail, use an App Password. Do not use your normal account password.

# If using port 465 (SSL/TLS): set MAIL_PORT=465 and the mail config will enable SSL
# For other providers (SendGrid, Mailgun, Postmark), replace server/port/credentials accordingly.

# Database example
# DATABASE_URL=mysql+pymysql://user:pass@localhost/emp_db

# JWT / Auth
# SECRET_KEY=replace_with_a_secure_random_value
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=60
# REFRESH_TOKEN_EXPIRE_DAYS=7

# Redis (optional)
# REDIS_HOST=localhost
# REDIS_PORT=6379
